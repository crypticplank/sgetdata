# This is a basic workflow that is manually triggered
name: CI
on:
  push: {}
  pull_request: {}
jobs:
  run:
    runs-on: macOS-latest
    name: Xcode ${{ matrix.xcode }}
    strategy:
      matrix:
        xcode: ["12"]
    steps:
    - uses: actions/checkout@master
    - name: Set Xcode
      run: |
        echo "Available Xcode versions:"
        ls /Applications | grep Xcode
        echo "Choosing Xcode_${{ matrix.xcode }}.app"
        sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app
        xcodebuild -version
        swift --version
        swift package --version
    - name: Checkout Project
      uses: actions/checkout@v1
    - name: Show the currently detailed version of Xcode for CLI
      run: xcode-select -p
    - name: Show Build Settings
      run: xcodebuild -project sgetdata.xcodeproj -scheme 'sgetdata' -showBuildSettings
    - name: Show Build SDK
      run: xcodebuild -project sgetdata.xcodeproj -scheme 'sgetdata' -showsdks
    - name: Show Available Destinations
      run: xcodebuild -project sgetdata.xcodeproj -scheme 'sgetdata' -showdestinations
    - name: build and test
      run: xcodebuild clean -project sgetdata.xcodeproj -scheme 'sgetdata' -destination 'platform=macOS' -showBuildTimingSummary
